/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['sjc.microlink.io', 'api.microlink.io'], // Adicionando os domínios permitidos
    },
  };
  
  export default nextConfig;
  