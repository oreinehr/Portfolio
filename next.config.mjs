/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['sjc.microlink.io', 'api.microlink.io', 'vercel.app', 'github.com'],
    },
  };
  
  export default nextConfig;
  